apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tor-proxy.fullname" . }}-test-connection"
  labels:
    {{- include "tor-proxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tor-proxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
