apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arbitrum-celestia.fullname" . }}-test-connection"
  labels:
    {{- include "arbitrum-celestia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: curl
      image: curlimages/curl
      command: ['curl']
      args:
        - --location
        - --request
        - POST
        - '{{ include "arbitrum-celestia.fullname" . }}:{{ .Values.service.ports.rpc.port }}/'
        - --header
        - 'Content-Type: application/json'
        - --data-raw
        - '{"jsonrpc":"2.0","method":"web3_clientVersion","params":[],"id":64}'
  restartPolicy: Never
