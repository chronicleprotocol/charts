apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helios.fullname" . }}-test-connection"
  labels:
    {{- include "helios.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helios.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
