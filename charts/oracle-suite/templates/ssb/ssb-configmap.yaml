kind: ConfigMap 
apiVersion: v1 
metadata:
  name: {{ include "oracle-suite.fullname" . }}-ssb
data:
  {{-/* Note, indent of only two spaces */}}
  config.json: |-
{{ .Files.Get "config.json" | indent 4 }}