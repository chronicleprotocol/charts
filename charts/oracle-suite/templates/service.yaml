apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-suite.fullname" . }}
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oracle-suite.selectorLabels" . | nindent 4 }}
