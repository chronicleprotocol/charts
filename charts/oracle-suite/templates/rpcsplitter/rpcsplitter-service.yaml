apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-suite.fullname" . }}-rpcsplitter
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.rpcsplitter.service.type }}
  ports:
    - port: {{ .Values.rpcsplitter.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    service: {{ include "oracle-suite.fullname" . }}-rpcsplitter
