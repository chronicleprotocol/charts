kind: ConfigMap 
apiVersion: v1 
metadata:
  name: {{ include "oracle-suite.fullname" . }}-rpcsplitter
data:
  {{-/* Note, indent of only two spaces */}}
  config.json: |-
{{ .Files.Get "splitter.json" | indent 4 }}