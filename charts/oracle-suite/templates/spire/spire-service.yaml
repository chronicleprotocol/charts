apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-suite.fullname" . }}-spire
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.spire.service.type }}
  ports:
    {{- range .Values.spire.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .name }}
      protocol: {{ .protocol }}}
      name: {{ .name }}
    {{- end }}
  selector:
    service: {{ include "oracle-suite.fullname" . }}-spire
