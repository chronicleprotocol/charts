apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-suite.fullname" . }}-omnia
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.omnia.service.type }}
  ports:
    - port: {{ .Values.omnia.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    service: {{ include "oracle-suite.fullname" . }}-omnia
