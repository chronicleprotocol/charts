apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oracle-suite.fullname" . }}-test-connection"
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oracle-suite.fullname" . }}:{{ .Values.ssb.service.port }}']
  restartPolicy: Never
