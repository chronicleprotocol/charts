apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-suite.fullname" . }}-torproxy
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.torproxy.service.type }}
  ports:
    - port: {{ .Values.torproxy.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    service: {{ include "oracle-suite.fullname" . }}-torproxy
