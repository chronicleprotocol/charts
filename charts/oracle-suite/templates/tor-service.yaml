apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-suite.fullname" . }}-tor
  labels:
    {{- include "oracle-suite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.tor.service.type }}
  ports:
    - port: {{ .Values.tor.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oracle-suite.selectorLabels" . | nindent 4 }}
