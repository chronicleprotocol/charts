apiVersion: v1
kind: Service
metadata:
  name: {{ include "optimism.fullname" . }}
  labels:
    {{- include "optimism.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "optimism.selectorLabels" . | nindent 4 }}
