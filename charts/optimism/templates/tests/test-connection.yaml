apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "optimism.fullname" . }}-test-connection"
  labels:
    {{- include "optimism.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "optimism.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
