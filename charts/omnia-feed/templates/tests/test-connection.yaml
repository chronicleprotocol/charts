apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omnia-feed.fullname" . }}-test-connection"
  labels:
    {{- include "omnia-feed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['https://icanhazip.com']
  restartPolicy: Never
