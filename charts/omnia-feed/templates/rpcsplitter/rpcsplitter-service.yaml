apiVersion: v1
kind: Service
metadata:
  name: {{ include "omnia-feed.fullname" . }}-rpcsplitter
  labels:
    {{- include "omnia-feed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.rpcsplitter.service.type }}
  ports:
    - port: {{ .Values.rpcsplitter.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    service: {{ include "omnia-feed.fullname" . }}-rpcsplitter
