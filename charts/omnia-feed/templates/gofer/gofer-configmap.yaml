apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "omnia-feed.fullname" . -}}-gofer-cm
data:
  {{- $files := .Files }}
  {{- range tuple "gofer.json.tpl" }}
  {{ . }}: |-
        {{ $files.Get . }}
  {{- end }}
