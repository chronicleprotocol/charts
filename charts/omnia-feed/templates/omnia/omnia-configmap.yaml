apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "omnia-feed.fullname" . -}}-omnia-cm
data:
data:
  {{- $files := .Files }}
  {{- range tuple "omnia.json.tpl" }}
  {{ . }}: |-
        {{ $files.Get . }}
  {{- end }}
