apiVersion: v1
kind: Service
metadata:
  name: {{ include "tweet-bot.fullname" . }}
  labels:
    {{- include "tweet-bot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tweet-bot.selectorLabels" . | nindent 4 }}
