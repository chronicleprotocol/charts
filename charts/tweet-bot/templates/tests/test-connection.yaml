apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tweet-bot.fullname" . }}-test-connection"
  labels:
    {{- include "tweet-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tweet-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
