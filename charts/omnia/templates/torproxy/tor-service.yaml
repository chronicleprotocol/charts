apiVersion: v1
kind: Service
metadata:
  name: {{ include "omnia.fullname" . }}-torproxy
  labels:
    {{- include "omnia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.torproxy.service.type }}
  ports:
    - port: {{ .Values.torproxy.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    service: {{ include "omnia.fullname" . }}-torproxy
