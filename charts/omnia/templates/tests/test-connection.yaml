apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "omnia.fullname" . }}-test-connection"
  labels:
    {{- include "omnia.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "omnia.fullname" . }}:{{ .Values.ssb.service.port }}']
  restartPolicy: Never
