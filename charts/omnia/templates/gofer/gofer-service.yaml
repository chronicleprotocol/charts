apiVersion: v1
kind: Service
metadata:
  name: {{ include "omnia.fullname" . }}-gofer
  labels:
    {{- include "omnia.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gofer.service.type }}
  ports:
    - port: {{ .Values.gofer.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    service: {{ include "omnia.fullname" . }}-gofer
