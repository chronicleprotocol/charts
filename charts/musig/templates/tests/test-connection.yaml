apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "musig.fullname" . }}-test-connection"
  labels:
    {{- include "musig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "musig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
